# Not used when running with Docker (use .env.dev / .env.prod).
# For local runs: copy to .env and set values.
PORT=3000
MONGODB_URI=mongodb://localhost:27017/multi-env-dev
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
